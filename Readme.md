## Median filter implementation (WIP)

Implementation for Median filter algorithm for CUDA**, CPU sequential and CPU parallel. (*) 

* Only for odd-size filters
** Only for grayscale images, that have size less than 65536

### Prerequisites 

1. OpenCV
2. CUDA and cuDNN
